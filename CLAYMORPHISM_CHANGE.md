# Claymorphism conversion

Visual-only conversion using the uploaded claymorphism HTML as the reference.
Only the `<style>` block in `flood_risk_watch.html` was changed. Existing HTML,
JavaScript, map behavior, live API integrations, CatBoost model, data files,
and backend files were preserved. The existing map tooltip styling was retained
and adapted to the clay variables.
